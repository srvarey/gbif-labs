<!DOCTYPE etl SYSTEM "http://scriptella.javaforge.com/dtd/etl.dtd">
<etl>    
    <properties>
        <include href="conf.properties"/>
    </properties>
    <connection id="old-registry" url="${old.registry.url}" user="${old.registry.user}" password="${old.registry.pwd}"/>
    <connection id="new-registry" url="${new.registry.url}" user="${new.registry.user}" password="${new.registry.pwd}"/>        

    <query id="endpoint_machine_tag" connection-id="old-registry">
        SELECT s.id AS endpoint_key,
        	t.id AS machine_tag_key 
        	FROM tag t
			JOIN agent a ON t.agent_id = a.id
			JOIN service s ON a.id = s.agent_id
			WHERE t.namespace_id IS NOT NULL
         			
		<script connection-id="new-registry">
		INSERT INTO endpoint_machine_tag(endpoint_key, machine_tag_key)
		VALUES (?endpoint_key,?machine_tag_key);
        </script>
    </query>
</etl>