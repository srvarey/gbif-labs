# TODO: use a test DB for the tests
locality.db.JDBC.driver=com.mysql.jdbc.Driver
locality.db.JDBC.url=${locality.db.url}
locality.db.JDBC.username=${locality.db.username}
locality.db.JDBC.password=${locality.db.password}